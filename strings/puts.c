#include<stdio.h>
#include<string.h>
int main(int argc, char const *argv[])
{
    char str[] ="College Wahllah is Best";

    //printf("%s", str);
    puts(str);
    puts("Hello Baby");
    return 0;
}